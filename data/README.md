Place the raw Kaggle CSV here as `star_classification.csv`.

Source: https://www.kaggle.com/datasets/fedesoriano/stellar-classification-dataset-sdss17

This directory's `.csv` files are gitignored (see project `.gitignore`) — the raw
100k-row export isn't committed, only the small held-out `test_data.csv` at the repo
root is.
